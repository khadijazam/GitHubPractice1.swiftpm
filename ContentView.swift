import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Khadija Azam")
            Text("Alex V")
            Image(systemName: "apple.terminal.on.rectangle")
                .foregroundStyle(.purple)
                .opacity(0.7)
        }
    }
}
